import { styled } from 'styled-components';

export const Main = styled.main`
    display: flex;
    width: calc(100vw - 6rem);
    flex-direction: row;
    align-items: center;
    gap: 50px;
    padding: 3rem;
`;